// ============================================================
// 페이지/레이아웃 상수 (index.html L4419–4507에서 추출)
// 웹앱 에디터 + Figma 내보내기 공통
// ============================================================

/** 기본 페이지(스트립) 너비 (px) */
export const PAGE_W = 720;

/** JPG 내보내기 분할 높이 (px) */
export const PAGE_H = 4000;

/** 에디터 최대 캔버스 높이 (단일 긴 페이지) */
export const PAGE_H_MAX = 999999;

/** 패널 간 기본 간격 (px) */
export const PANEL_GAP = 300;

/** 웹툰 기본 스트립 너비 (Figma 내보내기용) */
export const DEFAULT_STRIP_WIDTH = 800;

/** 패널 간 기본 여백 (Figma 내보내기용) */
export const DEFAULT_GUTTER = 20;

/** 씬 간 여백 배수 */
export const SCENE_GAP_MULTIPLIER = 3;
